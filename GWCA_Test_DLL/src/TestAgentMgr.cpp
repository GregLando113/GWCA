#include <GWCA\Managers\AgentMgr.h>

#include "GWCA_Tests.h"

//std::stack<std::function<bool()>> tests;

// on render


//
//TEST_CASE("AgentMgr") {
//	REQUIRE(GW::Agents::GetPlayerId() > 0);
//
//	assert(GW::Agents::GetPlayerId() == 0);
//}

/* 
brainstorming

assert that player exists, and properties make sense

loop over agents, assert that they make sense too

loop over player array, find ourselves

loop over npc array, check stuff

* select closest target
wait?
verify target change

*move
wait
verify movement

// test gosignpost ?

// test calltarget ?

// test getheroagentid ?

// test getagentname ?

*/
